spring.application.name=nicdocumentmanagementsystem
server.port=8088

# File upload directory
# NOTE: This path should exist on the live server where the application will be deployed.
file.upload-dir=/Users/macbook/Downloads/nicdocumentmanagementsystem/uploads

# --- Live Database Configuration ---
# MySQL connection URL for the live server
spring.datasource.url=jdbc:mysql://10.153.166.77:3306/nicdocmngsys?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Credentials
spring.datasource.username=testdbadmin
spring.datasource.password=JpAdmin@2024

# MySQL driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
# For a live server, 'validate' is safer than 'update' to prevent accidental schema changes.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Multipart File Upload Configuration
spring.servlet.multipart.max-file-size=3MB
spring.servlet.multipart.max-request-size=3MB
